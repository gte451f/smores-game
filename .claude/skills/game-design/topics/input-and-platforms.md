# Input and Platforms

**Status:** placeholder — not yet designed.

Will state the target input methods and platforms as an explicit design decision, rather
than leaving it scattered — touch is already supported at the implementation level (see
`game-systems`), and `player-experience.md`'s open questions defer "full controller-native
UI" to a later pass with keyboard/mouse as the initial target. This topic is where the
full target list (PC-only? console? which input methods at launch vs. later?) should be
stated in one place.

## Open Design Questions Worth Tracking

- Full platform target list isn't decided.
- Controller-native UI timing relative to initial release.
