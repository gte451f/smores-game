# Design Documentation

Game design and system documentation lives in `.claude/skills/` (`game-design` for
aspirational/intent-level design, `game-systems` for implemented systems and the player
surface). This folder now holds only:

- `plans/` — past AI-generated implementation plan documents that were eventually
  executed. Historical reference only; these are not maintained going forward.
